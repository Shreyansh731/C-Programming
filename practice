# include <stdio.h>
# include <math.h>
int main () {
int sunday , snowing ;
printf ("enter 1 if today is sunday outherwise 0\n");
scanf ("%d",&sunday);
printf ("enter 1 if today is snowing otherwise 0\n");
scanf ("%d",&snowing);
printf ("%d\n",sunday && snowing);
return 0;

}
